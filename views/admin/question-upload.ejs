<%- include('../partials/head', { title: 'Upload Questions' }) %>
<body class="min-h-screen bg-slate-50 text-gray-900">
  <%- include('../partials/navbar') %>
  <%- include('../partials/sidebar') %>

  <div class="md:ml-60 pt-14 min-h-screen page-body">
    <div class="page-header px-6 md:px-8 py-7 fade-up">
      <p class="text-white/50 text-xs font-semibold uppercase tracking-widest mb-1">Admin Panel</p>
      <h1 class="text-2xl md:text-3xl font-bold text-white">Upload Questions</h1>
      <p class="text-white/60 text-sm mt-1">Upload question images to the question bank</p>
    </div>

    <div class="p-6 md:p-8">
    <div class="grid grid-cols-1 lg:grid-cols-2 gap-6">

      <!-- Upload Form -->
      <div class="bg-white rounded-2xl shadow-sm border border-gray-100 lift p-6 fade-up">
        <form id="upload-form" class="space-y-4">

          <div>
            <label class="block text-sm font-medium text-gray-700 mb-1">Subject</label>
            <select id="sel-subject" required
                    class="w-full px-3 py-2 border border-gray-300 rounded-lg text-sm focus:ring-2 focus:ring-garud-accent outline-none">
              <option value="">Select Subject</option>
            </select>
          </div>

          <div>
            <label class="block text-sm font-medium text-gray-700 mb-1">Chapter</label>
            <select id="sel-chapter" required
                    class="w-full px-3 py-2 border border-gray-300 rounded-lg text-sm focus:ring-2 focus:ring-garud-accent outline-none">
              <option value="">Select Chapter</option>
            </select>
          </div>

          <div>
            <label class="block text-sm font-medium text-gray-700 mb-1">Topic</label>
            <select id="sel-topic" required
                    class="w-full px-3 py-2 border border-gray-300 rounded-lg text-sm focus:ring-2 focus:ring-garud-accent outline-none">
              <option value="">Select Topic</option>
            </select>
          </div>

          <!-- Question Type + Answer — only needed for Paste mode (auto-detected from filename in File mode) -->
          <div id="manual-answer-fields" class="hidden space-y-4">
            <div>
              <label class="block text-sm font-medium text-gray-700 mb-1">Question Type</label>
              <select id="sel-type"
                      class="w-full px-3 py-2 border border-gray-300 rounded-lg text-sm focus:ring-2 focus:ring-garud-accent outline-none">
                <option value="mcq">MCQ</option>
                <option value="numerical">Numerical</option>
              </select>
            </div>
            <!-- MCQ correct option -->
            <div id="mcq-options">
              <label class="block text-sm font-medium text-gray-700 mb-1">Correct Option</label>
              <select id="sel-correct-option"
                      class="w-full px-3 py-2 border border-gray-300 rounded-lg text-sm focus:ring-2 focus:ring-garud-accent outline-none">
                <option>A</option><option>B</option><option>C</option><option>D</option>
              </select>
            </div>
            <!-- Numerical answer -->
            <div id="numerical-options" class="hidden">
              <label class="block text-sm font-medium text-gray-700 mb-1">Correct Numerical Answer</label>
              <input id="inp-numerical" type="number" step="any"
                     class="w-full px-3 py-2 border border-gray-300 rounded-lg text-sm focus:ring-2 focus:ring-garud-accent outline-none"/>
            </div>
          </div>

          <!-- Image upload -->
          <div>
            <label class="block text-sm font-medium text-gray-700 mb-1">Question Image</label>

            <!-- Tab toggle -->
            <div class="flex rounded-lg border border-gray-200 overflow-hidden mb-3 text-sm font-medium">
              <button type="button" id="tab-file"
                      onclick="setImageTab('file')"
                      class="flex-1 py-1.5 bg-garud-accent text-white transition">&#x1F4C1; File</button>
              <button type="button" id="tab-paste"
                      onclick="setImageTab('paste')"
                      class="flex-1 py-1.5 bg-white text-gray-500 hover:bg-gray-50 transition">&#x1F4CB; Paste</button>
            </div>

            <!-- File upload -->
            <div id="zone-file">
              <!-- Naming convention hint -->
              <div class="mb-3 bg-blue-50 border border-blue-200 rounded-lg px-4 py-3 text-xs text-blue-700 space-y-1">
                <p class="font-semibold">&#x1F4DD; Filename format (type &amp; answer auto-detected):</p>
                <p><code class="bg-blue-100 px-1 rounded">1-A.jpg</code> &nbsp;→ Q1, MCQ, correct option <strong>A</strong></p>
                <p><code class="bg-blue-100 px-1 rounded">2-120.jpg</code> &nbsp;→ Q2, Numerical, answer <strong>120</strong></p>
                <p class="text-blue-500">Options: A B C D for MCQ &nbsp;|&nbsp; any number for Numerical</p>
              </div>
              <label for="question-image"
                     class="flex flex-col items-center justify-center border-2 border-dashed border-gray-300 rounded-lg p-6 cursor-pointer hover:border-garud-accent transition">
                <span class="text-3xl mb-2">&#x1F5BC;</span>
                <span class="text-sm text-gray-500">Click or drag — select multiple files</span>
                <input id="question-image" type="file" accept="image/*" multiple class="hidden"/>
              </label>
              <!-- Per-file parsed list -->
              <div id="file-list-preview" class="mt-3 space-y-1 max-h-48 overflow-y-auto"></div>
            </div>

            <!-- Paste zone -->
            <div id="zone-paste" class="hidden">
              <div id="paste-zone"
                   tabindex="0"
                   class="flex flex-col items-center justify-center border-2 border-dashed border-gray-300 rounded-lg p-6 cursor-text hover:border-garud-accent transition outline-none focus:border-garud-accent focus:ring-2 focus:ring-garud-accent/30"
                   title="Click here then Ctrl+V to paste an image">
                <span class="text-3xl mb-2">&#x1F4CB;</span>
                <span class="text-sm text-gray-500">Click here, then <kbd class="px-1.5 py-0.5 bg-gray-100 border border-gray-300 rounded text-xs font-mono">Ctrl+V</kbd> to paste</span>
                <span class="text-xs text-gray-400 mt-1">Works with screenshots &amp; copied images</span>
              </div>
            </div>

            <img id="image-preview" class="mt-3 max-h-48 rounded border hidden w-full object-contain bg-gray-50" alt="Preview"/>
            <p id="paste-filename" class="hidden text-xs text-gray-400 mt-1 text-center"></p>
          </div>

          <button type="submit" id="upload-btn"
                  class="w-full py-2.5 bg-garud-highlight text-white font-semibold rounded-lg hover:opacity-90 transition disabled:opacity-50">
            Upload Question
          </button>
        </form>
      </div>

      <!-- Recently uploaded -->
      <div class="bg-white rounded-2xl shadow-sm border border-gray-100 lift p-6 fade-up-1">
        <h2 class="text-lg font-bold text-gray-800 mb-4">Recently Uploaded</h2>
        <div id="recent-list" class="space-y-3 max-h-[500px] overflow-y-auto">
          <p class="text-sm text-gray-400">No uploads yet in this session.</p>
        </div>
      </div>
    </div>
    </div><!-- /inner -->
  </div>

  <%- include('../partials/toast') %>
  <script src="/js/api.js"></script>
  <script src="/js/toast.js"></script>
  <script src="/js/auth-guard.js"></script>
  <script src="/js/sidebar.js"></script>
  <script src="/js/pages/question-upload.js"></script>
</body>
</html>
