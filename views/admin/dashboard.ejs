<%- include('../partials/head', { title: 'Admin Dashboard' }) %>
<body class="min-h-screen bg-gray-50 text-gray-900">
  <%- include('../partials/navbar') %>
    <%- include('../partials/sidebar') %>

  <div class="md:ml-60 pt-14 min-h-screen">
    <div id="loading" class="flex items-center justify-center min-h-[60vh]">
      <div class="spinner"></div>
    </div>

    <div id="main-content" class="hidden p-6 max-w-7xl mx-auto">
      <h1 class="text-3xl font-bold text-gray-800 mb-8">Admin Dashboard</h1>

      <!-- Stat cards -->
      <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6" id="stat-cards"></div>

      <!-- Quick guide -->
      <div class="mt-10 bg-white rounded-xl shadow-md p-6">
        <h2 class="text-xl font-bold text-gray-800 mb-4">Quick Guide</h2>
        <div class="space-y-3 text-gray-600">
          <p><span class="font-semibold text-garud-accent">1.</span> Go to <span class="font-semibold">Question Bank</span> to manage Subjects â†’ Chapters â†’ Topics</p>
          <p><span class="font-semibold text-garud-accent">2.</span> Use the <span class="font-semibold">Upload</span> page to upload question images to Cloudinary</p>
          <p><span class="font-semibold text-garud-accent">3.</span> Create tests in the <span class="font-semibold">Tests</span> section with sections and assign questions</p>
          <p><span class="font-semibold text-garud-accent">4.</span> Publish tests to make them visible to students</p>
        </div>
      </div>
    </div>
  </div>

  <%- include('../partials/toast') %>
  <script src="/js/api.js"></script>
  <script src="/js/toast.js"></script>
  <script src="/js/auth-guard.js"></script>
  <script src="/js/sidebar.js"></script>
  <script src="/js/pages/admin-dashboard.js"></script>
</body>
</html>
