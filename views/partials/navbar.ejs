<!-- Top Navbar — fixed across all pages -->
<nav class="fixed top-0 left-0 md:left-60 right-0 h-14 bg-garud-dark/95 backdrop-blur-sm text-white flex items-center px-4 z-30 border-b border-white/10">

  <!-- Hamburger (mobile only — toggles sidebar) -->
  <button onclick="openSidebar()"
          class="md:hidden mr-3 p-1.5 rounded-lg hover:bg-white/10 transition"
          aria-label="Open menu">
    <svg xmlns="http://www.w3.org/2000/svg" class="w-5 h-5" fill="none"
         viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
      <path stroke-linecap="round" stroke-linejoin="round" d="M4 6h16M4 12h16M4 18h16"/>
    </svg>
  </button>

  <!-- Brand (mobile only) -->
  <a href="/" class="flex items-center gap-2 select-none md:hidden">
    <span class="text-lg font-extrabold tracking-wide text-white">GARUD <span class="text-garud-highlight">Classes</span></span>
  </a>

  <div class="flex-1"></div>

  <!-- User info (populated by sidebar.js → buildSidebar) -->
  <div id="navbar-user" class="hidden items-center gap-3">
    <div class="flex flex-col items-end leading-tight">
      <span id="navbar-username" class="text-sm font-semibold text-white"></span>
      <span id="navbar-role" class="text-xs text-white/40 capitalize"></span>
    </div>
    <div id="navbar-avatar"
         class="w-8 h-8 rounded-xl bg-garud-highlight flex items-center justify-center
                text-white font-bold text-sm flex-shrink-0">
    </div>
  </div>

</nav>
